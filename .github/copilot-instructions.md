# WeatherCheck workspace

- Frontend: React + Vite (JavaScript)
- Run with `npm install` then `npm run dev`
- Live weather uses Google Weather API; geocoding uses Open-Meteo
- Keep `.env.local` private; it is gitignored
